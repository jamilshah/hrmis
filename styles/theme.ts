import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f57c00', // Orange color similar to OrangeHRM
    },
  },
  typography: {
    fontFamily: 'Roboto, sans-serif',
  },
});

export default theme;